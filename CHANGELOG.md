# Changelog

| Date | Change | Details |
|------|--------|---------|
| 2026-05-20 | Add Medical Inquiries tab | New `DemoInquiryController` creates Case + Inquiry + InquiryQuestion records per HCP for the selected territory. Cases use the *Medical Inquiry* record type (looked up by Name so it works in orgs that namespace DeveloperName like `LSDO_Medical_Inquiry`). 1-2 cases per HCP, 1 Inquiry per Case (Status=Submitted, randomized channel + priority + disclaimer + submitted timestamp), 1-2 InquiryQuestions per Inquiry with realistic Immunexis/Immunonco clinical questions. Tagged via `SourceSystemName = 'AFLSCE-Demo-Data'`; cascade delete walks Questions → Inquiries → Cases. New `inquirySetup` LWC + tab; class added to `Demo_Data_StarterKit` permission set with object permissions for Case, Inquiry, InquiryQuestion. |
| 2026-05-19 | Per-HCP specialty overrides | `buildSpecialtyRecords` in `DemoAccountProviderController` now consults a `specialtyOverrides` map (keyed by HCP `Name`, e.g. `'Dr. Andrew Kim' => 'Immunology'`) before falling back to the round-robin rotation. Lets specific HCPs get story-consistent specialties without restructuring the rotation. |
| 2026-05-19 | Add Next Best Customer tab | New `DemoNextBestCustomerController` creates `TerritoryAccountScore` records ranking each demo HCP in a selected territory. `Rank` 1..N by Account Name; `TotalScore` descends 100→30 in 5-point steps; `ScoreExplainabilityInfo` carries a `rationals` JSON array (Account Profile, Activity Plan, Sales Performance, Account Scope, # of interactions in last 90 days) with tier (A/B/C) and segment derived from rank. Tagged via `SourceSystemName = 'AFLSCE-Demo-Data'` and `SourceSystemIdentifier = 'TAS-{territoryId}-{accountId}'`. Re-running Create clears existing demo scores for the territory first. New `nextBestCustomerSetup` LWC + tab in Demo Data Admin; class added to `Demo_Data_StarterKit` permission set with `TerritoryAccountScore` object permissions. |
| 2026-05-19 | Remove Campaigns tab and demo data | Campaign / CampaignMember are not enabled in every target org (e.g. orgs without the Marketing User feature license fail to compile `DemoCampaignController`). Deleted `DemoCampaignController.cls(-meta.xml)`, the `campaignSetup` LWC bundle, and the Campaigns tab from `demoDataAdmin.html`. Updated README tab order, removed Tab 10 section, Campaign tagging row, and tree references. |
| 2026-05-14 | Remove Field Sales Representative profile from repo | Profile metadata is org-specific and should not be deployed from this package. Removed `Field Sales Representative.profile-meta.xml`. |
| 2026-05-13 | Localize Product Guidance with country healthcare systems | Rewrote `getLocalizedMessages()` in `DemoVisitController` for all 7 international countries (GB, FR, DE, IT, ES, JP, KR) × 2 brands (Immunexis, Immunonco). Each country's 5 Messages + 2 Objectives now reference local regulatory bodies, formulary processes, and care pathways: GB (NICE TA, ICB formulary, BSRBR-RA, CDF, NHS homecare, MDT, Genomic Lab Hub), FR (HAS/ASMR, livret thérapeutique, AP-HP, SFR/ORA, INCa, ATU, GHT, CLCC), DE (G-BA/AMNOG, DGRh, RABBIT, KV, NUB, DKG-Zentren), IT (AIFA Classe A/H, PTR/PTO, Registro AIFA, Rete Oncologica, Legge 648, DPC/ASL), ES (AEMPS/IPT, CIPM, BIOBADASER, SERMAS/CatSalut/SAS, SEOM, uso compasivo), JP (PMDA, NHI薬価収載, 中医協, 薬事委員会, がんゲノム医療, DPC/PDPS), KR (MFDS, NHIS/HIRA, 약사위원회, KCR, KSMO, 위험분담제/RSA, DUR). |
| 2026-05-13 | Fix CPA delete — clear Visit.PlaceId and recreate PATIs | `deleteContactPointBatch('ContactPointAddress')` now: (1) nulls `Visit.PlaceId` on visits referencing demo CPAs, (2) backs up and deletes blocking `ProviderAcctTerritoryInfo` records (nulling `PreferredAddressId` didn't work — the LSC managed trigger re-populates it), (3) deletes the CPAs, (4) recreates PATIs without `PreferredAddressId` to preserve territory assignments. Eliminates the need to re-run "Assign Territories" after deleting addresses. |
| 2026-05-13 | Localize medical insights for all country healthcare systems | Rewrote `DemoMedicalInsightLocale` insight templates for IT (AIFA/SSN/ASL/PTR-PTO/Registro), ES (SNS/AEMPS/CIPM/IPT/CCAA/BIOBADASER), JP (NHI/PMDA/中医協/薬事委員会/JCR/DPC), KR (NHIS/HIRA/RSA/DUR/KCR/KSMO). Added structural accounts: FR (AP-HP, ARS Ile-de-France, ARS Auvergne-Rhône-Alpes, CPAM), IT (ASL Milano, ASL Napoli 1 Centro), ES (SERMAS, CatSalut, SAS). Updated test class with coverage for territory methods and all 9 locale variants. |
| 2026-05-12 | Localize GB/FR/DE medical insights and add NHS accounts | Rewrote GB insight templates with NHS-specific scenarios (NICE TA, ICB formulary, shared care protocols, traffic light reclassification, QOF alignment, BSRBR registry, biosimilar switching, homecare). Rewrote FR templates (HAS/ASMR, livret thérapeutique, ARS, IQVIA, HAD, liste en sus, RCP, INCa). Rewrote DE templates (AMNOG/G-BA, Krankenkassen, KV, Arzneimittelkommission, RABBIT, Selektivvertrag, DKG). Added GB structural accounts (NHS Trusts, ICBs, GP Surgeries). Added territory picker and per-HCP insight creation. Added US prescribing behavior insights (NRx/TRx with trends). Created healthcare guide docs for GB, FR, IT, ES. |
| 2026-05-12 | Add Medical Insights tab | New `DemoMedicalInsightController` creates MedicalInsight records (1-2 per brand per HCP) with realistic clinical content for Immunexis (zelanumab) and Immunonco (vorastinib). Links to HCP accounts via MedicalInsightAccount and brand products via MedicalInsightProduct. Shared with territory groups (Private OWD). `DemoMedicalInsightLocale` provides 10 Immunexis + 10 Immunonco templates per language (9 languages). US territories also get prescribing behavior insights with monthly NRx/TRx numbers and trend. Territory picker with create/delete per territory. Tagged via Name suffix ` #AFLSCE`. |
| 2026-05-11 | Share ProductGuidance with territory groups | `createProductGuidance()` now creates `ProductGuidanceShare` records for each country's leaf territory groups after insert (Private OWD). Status line on Product Alignment tab includes Product Guidance count via `DemoProductAlignmentController.getStatus()`. Fixes blank "Message Record" on ProviderVisitDtlProductMsg when viewed as field rep. |
| 2026-05-11 | Add Product Guidance to Product Alignment tab | New `createProductGuidance()` and `deleteProductGuidance()` in `DemoVisitController` create 154 ProductGuidance records (7 per country brand: 5 Messages + 2 Objectives). Messages: Efficacy, Safety, Differentiation, Patient Experience, Convenience. Objectives: Positioning, Patient Identification. All localized in native language (FR, DE, IT, ES, JP, KR, PT). Buttons on the Product Alignment tab LWC. |
| 2026-05-08 | Share HealthcareProvider with territory groups + progress UI | "Assign Territories" now shares HCP records with territory groups (via `HealthcareProviderShare`) so field reps can see them. Split into 5 sequential steps with activity log progress (cleanup → account OTA → HCP sharing → PATI → summaries). Also sets `IsPrimaryProvider = true` on all new HealthcareProvider records. |
| 2026-05-04 | Add Campaigns tab for HCP Journey app | New `DemoCampaignController` creates journey campaigns (1 per country with Unaware→Advocate stages, all HCPs as members) and 7 marketing campaigns (email nurtures, webinars, congress follow-ups with Sent/Opened/Clicked/etc. statuses). Tagged via `External_ID__c = AFLSCE-Demo-Data-CAMP-*`. |
| 2026-05-01 | Remove template dependency from Action Plans | `DemoActionPlanBatch` no longer requires specific named templates in Final status. Uses any existing Final `ActionPlanTemplateVersion` to satisfy the required field, then replaces auto-created tasks with custom `KAM_TASKS` from `DemoAccountPlanData`. Added `Demo_Account_Plan` permission set with `ActionPlansUserAccess` and CRUD on AccountPlan, ActionPlan, etc. |
| 2026-05-01 | Planned visits for next 30 days | Visit creation now adds ~50 planned visits (Status=Planned) for today through today+30 days. Inserted as Completed first to bypass `ProviderVisitTrigger` email templates, ProviderVisit and product detailing created, then flipped to Planned. |
| 2026-05-01 | Organic visit distribution with phase-based weighting | Replaced per-week random weights with multi-week phases (3-6 alternating hot 1.6-2.5x and cold 0-0.4x streaks). Cumulative progress chart now shows realistic deviations from the target line instead of tracking it linearly. |
| 2026-05-01 | Measure type picker for Activity Plans | Added `getMeasureTypeOptions()` querying `ProviderActivityMeasureType` and `lightning-radio-group` on the Activity Plans tab. Primary measure type gets 90% of visit goals. Renamed tab from "Provider Activity Plans" to "Activity Plans". |
| 2026-05-01 | Channel picker for visit creation | Replaced activity-plan-goal-based channel distribution with a UI radio button group. `getChannelOptions` returns active `Visit.Channel` picklist values; `createVisits` accepts `primaryChannel` (90% of 600 yearly visits). Auto-defaults to "In-Person". Remaining 10% split evenly across other channels. |
| 2026-05-01 | Fix visit date clustering on Tue/Thu | Replaced pure LCG-based `pickVisitDates` with evenly-spaced base indices plus small random jitter, distributing visits across all 5 weekdays. |
| 2026-05-01 | Delete confirmation on all tabs | Added `window.confirm` dialog to all 10 `handleDelete` methods across all LWC tabs to prevent accidental data deletion. |
| 2026-05-01 | Expand major city HCPs to 20 | Added 160 doctors and 20 orgs across London, Paris, Berlin, Rome, Madrid, Tokyo, Seoul, São Paulo, Mexico City, Buenos Aires. Each major city now has 20 HCPs and 5-8 HCOs. |
| 2026-05-01 | Lower activity plan goals to 10–50 per HCP | Changed `goalValues` from 600/500/400… to 50/48/45/42/40…/12/10 for realistic per-HCP visit targets. |
| 2026-05-01 | Fix NPI uniqueness across batches | `buildNpiRecords` now accepts an offset parameter so each batch generates unique NPI numbers (batch 1 starts at 1000000000, batch 2 at 1000000030, etc.). Prevents "NPI value must be unique" errors when US providers are created in multiple batches. |
| 2026-05-01 | Batched visit deletion with unlock and error reporting | Replaced single `deleteVisits()` with `deleteVisitBatch()` that returns remaining count. LWC loops batches until done. Unlocks visits (Status→Planned) before delete, removes child ProviderVisitProdDetailing and ProviderVisit first, reports per-batch errors, and breaks if remaining count stops decreasing. Added UI warnings about disabling VisitLockHandler and RemoteSessionInvitationVisitHandler triggers before deleting. |
| 2026-05-01 | Per-country batched account/provider creation | Split `createAccountsAndProviders` into 5 separate Apex methods (`getCountryCodes`, `getDoctorCount`, `createOrgsForCountry`, `createDoctorsForCountry`, `createProvidersForCountry`, `createAffiliationsForCountry`) each running as a separate transaction. LWC loops through countries with batched provider (30) and affiliation (20) calls. Avoids SOQL 101 governor limits caused by LSC managed-package trigger overhead. Activity log shows per-country, per-batch progress in the UI. |
| 2026-05-01 | NPI records restricted to US providers only | `createProvidersForCountry` only creates HealthcareProviderNpi records when `countryCode == 'US'`. International providers skip NPI generation. |
| 2026-04-30 | Activity Plan & Visit troubleshooting guide | New `docs/ActivityPlan-Guide.md` documenting ProviderActivityMeasureType Category requirement, Visit object quirks (InstructionDescription tagging, TerritoryId+Channel for calendar, VisitSubmitDateTime for progress chart), VisitLockHandler delete blocking, timezone-aware scheduling, batch job behavior, and stuck job clearing (must reset both JobId and JobStatus). |
| 2026-04-30 | Move "Clear Stuck Job Status" to Provider Activity Plans tab | Moved `clearActivityPlanJobStatus()` from `DemoVisitController` to `DemoActivityPlanController`. Updated method to clear both `JobId` and `JobStatus` fields (clearing only JobStatus was insufficient when old JobId was stale). Removed button from visitSetup LWC, added to providerActivityPlanSetup LWC. |
| 2026-04-30 | Add Visits tab with territory-scoped visit creation | New `DemoVisitController` creates 3-6 completed Visits per weekday for the last 30 days with timezone-aware scheduling (8am-4pm local). Each Visit gets a ProviderVisit (with VisitSubmitDateTime for activity plan progress) and ProviderVisitProdDetailing records for Immunexis + Immunonco brands. Territory picker, delete with VisitLockHandler warning. |
| 2026-04-30 | Add Activity Plans tab with product-level goal measures | New `DemoActivityPlanController` creates TimePeriod, ActivityPlan, ActivityPlanTerritory, ProviderActivityGoal, and ProviderActivityGoalMeasure records per territory. Visit-level measures (In Person, Phone, Remote) split from NonProductGoal (60%). Product-level measures track Immunexis and Immunonco brands per country from ProductLevelGoal (40%), with `ProductList` linking to country-specific `LifeSciMarketableProduct` brand records. |
| 2026-04-30 | Rename DemoActivityPlanController → DemoAccountPlanController | The old `DemoActivityPlanController` (which creates AccountPlans + ActionPlans) was misnamed. Renamed to `DemoAccountPlanController` with methods `createAccountPlansAndActions` / `deleteAccountPlansAndActions`. `DemoActivityPlanDeleteBatch` → `DemoAccountPlanDeleteBatch`. Updated `activityPlanSetup` LWC and `DemoAccountPlanLocale` references. |
| 2026-04-30 | ProviderAffiliation FLS for Field Sales Representative | Added read access on all 18 ProviderAffiliation fields to the Field Sales Representative profile. Missing FLS caused "No such column 'RelatedAccountName'" error on the Affiliations tab. |
| 2026-04-30 | Affiliation OwnerId set to territory user | `DemoAffiliationHelper.createAffiliations()` now accepts `cityTerrMap`, resolves each doctor's territory via `UserTerritory2Association`, and sets `OwnerId` to the territory user. Previously all affiliations were owned by the admin, making them invisible to field reps due to sharing. |
| 2026-04-30 | Double affiliations via cross-city matching | Each HCP doctor is now affiliated with hospitals/clinics in their own city plus one neighboring city in the same country (round-robin). Increases affiliation count from ~550 to ~1,026. |
| 2026-04-29 | Reduce international territories from 5 to 3 per country | GB (London, Manchester, Edinburgh), FR (Paris, Lyon, Marseille), DE (Berlin, Munich, Hamburg), IT (Rome, Milan, Naples), ES (Madrid, Barcelona, Seville), JP (Tokyo, Osaka, Kyoto), KR (Seoul, Busan, Incheon), BR (São Paulo, Rio de Janeiro, Porto Alegre), MX (Mexico City, Guadalajara, Monterrey), AR (Buenos Aires, Córdoba, Rosario). Dropped cities: Birmingham, Cardiff, Toulouse, Lille, Frankfurt, Dusseldorf, Venice, Florence, Valencia, Bilbao, Nagoya, Yokohama, Daejeon, Seongnam, Belo Horizonte, Salvador, Puebla, Merida, La Plata, Tucumán. |
| 2026-04-29 | Enforce minimum 4 HCPs + 2 HCOs per territory | Added hospitals and doctors to all international territories that were below threshold. Every leaf territory now has at least 2 hospital/clinic HCOs and 4 HCP person accounts. |
| 2026-04-29 | Dynamic city-based affiliations | Rewrote `DemoAffiliationHelper` to dynamically affiliate each HCP with every hospital/clinic/medical center in the same city (by BillingCity + StateCode + CountryCode), replacing hardcoded 1:1 pairs. Produces realistic many-to-many relationships. |
| 2026-04-29 | Territory Data Summary table | Added a summary table to the Accounts & Providers tab showing HCPs, HCOs, PATI, PATS, and affiliations per leaf territory with totals row. Refreshes after create, assign, and delete operations. |
| 2026-04-29 | Assigned user shown in territory tree | `getHierarchy()` now queries `UserTerritory2Association` and appends the assigned user's name to each territory node label in the tree view. |
| 2026-04-29 | Updated city-territory mapping | `buildCityTerritoryMap()` cleaned up to remove all dropped cities (Cambridge, Villejuif, Heidelberg, Heerlen, Padua, Florence, Valencia, Kamogawa, Seongnam, Belo Horizonte, Salvador, Puebla, Pilar, Brasília) and add new city mappings (Edinburgh, Marseille, Incheon). |
| 2026-04-29 | Fix orphaned BusinessLicense records | Diagnosed and fixed state licenses created without `AccountId`, which caused red license indicators on the mobile account list despite green visit-level checks. |
| 2026-04-29 | Fix DemoActionPlanBatch test constructor | Updated test to pass all 4 required arguments (`engTemplateMap`, `startDate`, `engToLocalName`, `engToLocalTasks`). |
| 2026-04-29 | Documented DCR blocker for PersonAccount creation | Data Change Requests in LSC Admin Console block PersonAccount DML — must be disabled before running "Create Accounts & Providers". |
